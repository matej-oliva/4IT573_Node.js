<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo Detail</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <div class="d-flex flex-row align-items-center">
        <a href="/" class="btn btn-secondary">Return</a> 
        <h1 class="mx-3 card-title"><%= todo.title %></h1>
      </div>

      <div class="card my-3">
        <div class="card-body">
          <p class="card-text" id="description"><%= todo.description %></p>
          <a class="btn btn-info" href="/change-todo-state/<%= todo.id %>">
            <%= todo.done ? 'Splneno' : 'Nesplneno' %>
          </a>
          <a class="btn btn-danger" href="/remove-todo/<%= todo.id %>">
            Odebrat
          </a>
        </div>
      </div>

      <h2>Upravit Todo</h2>
      <form action="/edit-todo/<%= todo.id %>" method="post">
        <div class="mb-3">
          <label for="title" class="form-label">Nový název</label>
          <input
            type="text"
            class="form-control"
            id="title"
            name="title"
            value="<%= todo.title %>"
          />
        </div>
        <div class="mb-3">
          <label for="description" class="form-label">Nový popis</label>
          <textarea class="form-control" id="description" name="description" rows="4"><%= todo.description %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Uložit</button>
      </form>
      
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
    ></script>
  </body>
</html>
